<div class="jumbotron">
        <h1 class="display-4">My Groups</h1>
        <hr class="my-4">
        <% if(currentUser.groups_ids.length === 0) { %>
        <p>You currently don't belong to any group.</p>
        <% } else { %>

        <% } %>
        <hr class="my-4">
        <a class="btn btn-primary btn-lg" href="group/join" role="button">Join A Group</a>
</div> 